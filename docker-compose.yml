name: pau

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: pau
      POSTGRES_USER: pau
      POSTGRES_PASSWORD: pau
    ports:
      - "54329:5432"
    volumes:
      - pau-postgres:/var/lib/postgresql/data

volumes:
  pau-postgres:
